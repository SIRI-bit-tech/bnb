/**
 * Deprecated (kept to avoid breaking route structure).
 * Admin chrome now lives in `app/admin/layout.tsx` for consistent sidebar/header.
 */
export default function AdminDashboardLayout({ children }: { children: React.ReactNode }) {
  return children
}
